export interface IDatum {
  title: string,
  datum: string|number
}

export interface IRocket {
  name: string,
  active: boolean,
  firstFlight: string,
  description: string,
  wikipedia: string,
  image: string,
}